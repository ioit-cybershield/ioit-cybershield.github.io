---
import Navbar from '@/components/general/Navbar';
import Layout from '../layouts/Layout.astro';
import HeroSection from '@/components/landing/Hero';
import AboutSection from '@/components/landing/About';
import LenisProvider from '@/providers/lenis-provider';
import TimelineSpinner from '@/components/landing/Timeline';
import Gallery from "@/components/landing/Gallery"
import Footer from "@/components/general/Footer"
import Preloader from '@/components/ui/preloader';
import CTA from '@/components/landing/CTA';
---

<Layout>
  <Preloader client:load />
  <LenisProvider client:load>
    <Navbar client:idle={{timeout:200}}/>
    <HeroSection client:idle={{timeout:300}}/>
    <AboutSection client:visible={{rootMargin: "200px"}} />
    <Gallery client:visible={{rootMargin: "200px"}}/>
    <TimelineSpinner client:load />
    <CTA client:visible={{rootMargin: "200px"}}/>
    <Footer client:visible={{rootMargin: "200px"}} />
  </LenisProvider>
</Layout>
